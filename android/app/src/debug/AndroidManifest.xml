<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed only for Flutter tooling (hot reload, breakpoints, observatory). -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
