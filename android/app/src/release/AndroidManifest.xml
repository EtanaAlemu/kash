<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Crashlytics needs network to upload crash reports. App data stays on-device. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
